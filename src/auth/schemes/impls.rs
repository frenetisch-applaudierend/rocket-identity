/// This module contains common imports for authentication schemes.
pub mod prelude {
    pub use crate::{
        AuthenticationError, AuthenticationScheme, ClaimValue, Claims, Outcome, PasswordHash,
        Roles, Services, User,
    };
}
